// The header card and each info section (about, admin, federation, service
// classes, addons, themes) are registered as widgets in ../index.ts (header /
// contentTop slots) and rendered by Layout.tsx's <Slot .../> above this view
// — see hq/views/HqView.tsx for the same pattern.
export default function SiteinfoView() {
  return null;
}
